package com.qa.databases.interfaces;

/**
 * create CRUD functionality that allows for a parameter and int return type
 */
public interface CreateParam {
    public int create(int orderID);
}
